import { style } from '@vanilla-extract/css';

export const root = style({ padding: '1rem', background: '#f0f0f0', borderRadius: 8, margin: '1rem 0' });
export const gameState = style({ background: '#fff', padding: '1rem', margin: '1rem 0', borderRadius: 4, border: '1px solid #ccc' });
export const controls = style({ margin: '1rem 0' });
export const results = style({ marginTop: '1rem' });
export const resultItem = style({ background: '#fff', padding: '0.5rem', margin: '0.25rem 0', borderRadius: 4, borderLeft: '4px solid #007bff' });


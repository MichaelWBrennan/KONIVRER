import { style } from '@vanilla-extract/css';

export const zoneLabel = style({ position: 'absolute', top: -15, left: 5, color: 'rgba(255,255,255,0.7)', fontWeight: 'bold', pointerEvents: 'none' });
export const countBadge = style({ position: 'absolute', bottom: 2, right: 2, backgroundColor: 'rgba(0,0,0,0.8)', color: 'white', padding: '2px 4px', borderRadius: 2, fontWeight: 'bold' });


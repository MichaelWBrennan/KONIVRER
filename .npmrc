# Prevent node-sass installation and compilation
node-sass_binary_site=false
node-sass_binary_cache_path=false
skip_node_sass_tests=true

# Use npm registry
registry=https://registry.npmjs.org/

# Disable optional dependencies that might pull in node-sass
optional=false

# Force npm to use overrides
legacy-peer-deps=false
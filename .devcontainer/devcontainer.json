{
  "name": "openhands-codespace",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--init"],
  "forwardPorts": [3000],
  "postCreateCommand": "docker compose up -d",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
